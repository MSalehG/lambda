This repository is for a Data Engineering project implementing a Lambda architecture involving a batch ETL pipeline and a streaming ETL pipeline using pyspark. The batch data is read from a CSV source and the streaming data is fetched from Kafka. The results are saved into a PostgreSQL database.
